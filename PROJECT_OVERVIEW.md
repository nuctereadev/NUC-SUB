# NUC-SUB — توضیح کامل پروژه

> نسخهٔ بررسی: `2.0.1` · مخزن: `github.com/nuctereadev/NUC-SUB`

این سند برای ارائه به کارفرما، مشتری یا هر هوش مصنوعی دیگری نوشته شده تا در یک
نگاه بفهمد **NUC-SUB چیست، چه مسئلهای را حل میکند، چطور کار میکند و از چه
اجزایی تشکیل شده است.**

---

## ۱) چکیده — پروژه یک خطی

NUC-SUB یک **موتور قالب (Theme Engine) برای صفحهٔ اشتراک (Subscription Page)**
پنل‌های VPN است. آن را روی پنل 3x-ui (سنایی) یا Pasarguard نصب می‌کنید و به‌جای
صفحهٔ پیش‌فرض و سادهٔ پنل، صفحه‌ای مدرن و قابل‌انتخاب (از ۸ قالب) به کاربران
نمایش داده می‌شود — بدون اینکه به نحوهٔ توزیع کانفیگ VPN یا امنیت پنل دست بزند.

- CLI سبک و خودکار به زبان Bash
- وب‌پنل مدیریت اختیاری برای 3x-ui (Python استاندارد، بدون وابستگی خارجی)
- ۸ قالب آماده برای هر پنل (در مجموع ۱۶ قالب)
- قابلیت تنظیم لینک کانال تلگرام که داخل همهٔ قالب‌ها نمایش داده شود

---

## ۲) مسئله و انگیزه (چرا این پروژه وجود دارد؟)

وقتی یک ارائه‌دهندهٔ VPN، لینک اشتراک (Subscription URL) را به کاربر می‌دهد،
کاربر با مرورگر آن را باز می‌کند. مرورگر از پنل، صفحه‌ای می‌گیرد که شامل این
اطلاعات است:

- وضعیت اشتراک (فعال/خراب)
- مصرف ترافیک (دانلود و آپلود، کل، باقی‌مانده)
- تاریخ انقضا
- عنوان و پیام پنل
- لیست لینک‌های کانفیگ + کپی هرکدام یا همهٔ آن‌ها

مشکل: **صفحهٔ پیش‌فرض این پنل‌ها ظاهر قدیمی، انگلیسی‌محور و یکدست با همهٔ
ارائه‌دهنده‌هاست.** برندسازی و تجربهٔ کاربری پایین است. تغییر دستی این صفحه‌ها
سخت، شکننده و مخصوص هر پنل است.

راه‌حل NUC-SUB: نصب یک‌دستوری + انتخاب قالب از CLI یا وب‌پنل + اعمال فوری،
به‌طوری‌که فقط **صفحهٔ انسانی (HTML)** عوض شود و **کانفیگ اشتراک دست‌نخورده
بماند.**

---

## ۳) پنل‌های پشتیبان‌شده

| پنل | مکانیزم تغییر قالب | فرمت قالب |
|---|---|---|
| **3x-ui (سنایی)** | کلید `subThemeDir` در دیتابیس sqlite پنل (`/etc/x-ui/x-ui.db`) | Go `html/template` |
| **Pasarguard** | متغیر `SUBSCRIPTION_PAGE_TEMPLATE` در فایل `.env` پنل | Jinja2 (یک فایل مستقل) |

تشخیص خودکار پنل: فایل `panel.conf` ساخته‌شده توسط نصب‌کننده، یا در نبود آن،
بررسی دیسک برای دیتابیس x-ui یا پوشهٔ Pasarguard.

---

## ۴) معماری و نقشهٔ پروژه

```
NUC-SUB/
├── install.sh                 ← نصب‌کنندهٔ یک‌دستوری (bash)
├── cli/nucsub                 ← CLI و منوی تعاملی (bash، هستهٔ پروژه، ~۱۲۰۰ خط)
├── webpanel/                  ← وب‌پنل مدیریت اختیاری (فقط 3x-ui)
│   ├── server.py              ← سرور HTTP با کتابخانهٔ استاندارد Python3 (بدون pip)
│   ├── index.html             ← رابط کاربری خالص HTML/JS/CSS
│   ├── css/ fonts/ fa/        ← فونت‌ها و آیکون‌های محلی (بدون CDN)
├── themes/<name>/             ← ۸ قالب 3x-ui (Go html/template)
├── pasarguard-themes/subscription/<name>.html ← ۸ قالب Pasarguard (Jinja2)
├── LICENSE                    ← MIT
└── README.md
```

بخش‌های نصب‌شده روی سرور:

```
/opt/nuc-sub/
├── cli/nucsub                 ← CLI (با نام مستعار nucsub در PATH)
├── themes/                    ← قالب‌های 3x-ui
├── pasarguard-themes/         ← قالب‌های Pasarguard
├── webpanel/                  ← فایل‌های وب‌پنل
├── config.json                ← تنظیمات پروژه (مثل لینک تلگرام)
├── panel.conf                 ← نوع پنل (3xui / pasarguard)
├── .webpanel-token            ← توکن وب‌پنل (تنظیم 0600)
└── .webpanel-env              ← متغیرهای محیطی سرویس (تنظیم 0600)
```

نصب محلی خدمات:

- قالب 3x-ui → `/etc/x-ui/sub_templates/<نام>`
- قالب Pasarguard → `/var/lib/pasarguard/templates/subscription/<نام>.html`
- سرویس systemd وب‌پنل → `xui-sub-panel`

---

## ۵) نحوهٔ کار — گام به گام

### ۵-۱ سناریوی 3x-ui (رایج‌ترین)

1. کاربر دستور `nucsub apply arctic` را اجرا می‌کند (یا از منو/وب‌پنل انتخاب می‌کند).
2. اگر قالب روی دیسک نباشد، به‌صورت **تقاضامحور (on-demand)** از مخزن GitHub
   دانلود می‌شود (دانلود موازی ۶ فایل با time-out تا روی اینترنت کند قفل نشود).
3. فایل‌های قالب (HTML/CSS/JS/Font/Icons) به `/etc/x-ui/sub_templates/arctic/`
   کپی می‌شوند.
4. placeholder لینک تلگرام (`@@TG_CHANNEL@@`) با مقدار `config.json` جایگزین می‌شود.
5. مسیر تم در تنظیمات پنل (دیتابیس x-ui، کلید `subThemeDir`) نوشته می‌شود.
6. سرویس `x-ui` ری‌استارت می‌شود تا صفحهٔ جدید فوراً سرو شود.

> نکتهٔ مهم: پنل همچنان به کاربر کانفیگ اشتراک می‌دهد؛ فقط قالب HTML صفحهٔ انسانی
> عوض می‌شود. لینک اشتراک، ساختار کانفیگ و سرویس‌ها دست‌نخورده‌اند.

### ۵-۲ سناریوی Pasarguard

1. قالب Jinja2 (مثلاً `volt.html`) به پوشهٔ قالب‌های پنل کپی می‌شود.
2. `SUBSCRIPTION_PAGE_TEMPLATE` در `.env` پنل روی همان فایل تنظیم می‌شود.
3. در صورت نیاز پنل ری‌استارت/اعمال می‌شود.

### ۵-۳ قابلیت لینک تلگرام (چنل)

- مقدار `telegram_channel` در `/opt/nuc-sub/config.json` ذخیره می‌شود.
- هر قالب یک placeholder `@@TG_CHANNEL@@` دارد که هنگام deploy با مقدار واقعی
  جایگزین می‌شود (هر دو در HTML و در جاواسکریپت کنترل نمایش).
- `nucsub telegram get|set|clear` چنل را می‌خواند/تغییر می‌دهد/حذف می‌کند و
  **فوراً روی تم فعالِ deploy شده اعمال می‌کند** (بدون نیاز به تکرار apply).
- وب‌پنل هم وقتی چنل ذخیره می‌شود همین مسیر را می‌رود، پس تم آیندهٔ فوری به‌روز می‌شود.
- اگر چنل خالی باشد، دکمه با آیکون تلگرام داخل قالب مخفی می‌شود؛ اگر پر باشد
  نمایش داده می‌شود (کنترل با JS در همان فایل قالب).

### ۵-۴ مسیر دادهٔ قالب 3x-ui

پنل، هنگام render صفحهٔ اشتراک، یک ساختار دادهٔ Go را داخل قالب می‌ریزد:

| فیلد Go | معنا |
|---|---|
| `.sId` | شناسهٔ اشتراک |
| `.enabled` | فعال بودن اشتراک |
| `.download` / `.upload` | ترافیک مصرف‌شده (فرمت‌شده) |
| `.total` / `.used` / `.remained` | ترافیک کل، مصرفی، باقی‌مانده |
| `.expire` | زمان انقضا (ثانیه، ۰ = نامحدود) |
| `.subTitle` / `.subSupportUrl` / `.announce` | عنوان، لینک پشتیبانی، اعلان |
| `.links` / `.emails` | لیست لینک‌ها و ایمیل‌های مرتبط |

هر قالب 3x-ui مستقل است (فونت‌ها و آیکون‌ها به‌صورت محلی سرو می‌شوند، بدون
درخواست CDN خارجی) و همین ساختار را با ظاهری متفاوت رندر می‌کند.

---

## ۶) اجزای نرم‌افزاری

### ۶-۱ CLI (`cli/nucsub`)

نقطهٔ مرکزی کنترل. همهٔ کارها را بدون تعامل یا به‌صورت منوی تعاملی انجام
می‌دهد. دستورها:

| دستور | کار |
|---|---|
| `nucsub list` | فهرست قالب‌ها با وضعیت دانلود و فعلی |
| `nucsub apply <name>` | دانلود (در صورت نیاز) و فعال‌سازی قالب |
| `nucsub reset` | بازگشت به صفحهٔ پیش‌فرض پنل |
| `nucsub remove <name>` | حذف کامل یک قالب |
| `nucsub status` | نمایش پنل، دیتابیس، مسیرها و وضعیت سرویس |
| `nucsub webpanel ...` | مدیریت وب‌پنل (`start|stop|restart|status|token`) |
| `nucsub telegram ...` | نمایش/ست/حذف چنل تلگرام (`get|set|clear`) |
| `nucsub update` | دریافت آخرین نسخه از GitHub و نصب مجدد |
| `nucsub uninstall` | حذف کامل همه‌چیز (CLI، قالب‌ها، وب‌پنل، تنظیم DB) |
| `nucsub menu` | منوی تعاملی (پیش‌فرض) |
| `nucsub version` | نمایش نسخه |

منوی تعاملی:

```
1) Apply theme    4) Web panel      7) Status & info
2) Reset to       5) Update NUC-SUB 0) Exit
   default        6) Uninstall
3) Remove theme
```

زیر منوی **Web panel** گزینهٔ **Telegram channel** هم هست: نمایش چنل فعلی،
ست/تغییر، یا حذف آن — تا مدیریت چنل کاملاً از ترمینال هم ممکن باشد.

### ۶-۲ وب‌پنل (`webpanel/`, فقط 3x-ui)

- سرور HTTP با **کتابخانهٔ استاندارد Python3** (بدون وابستگی خارجی، بدون pip).
- رابط کاربری خالص HTML/JS/CSS با فونت‌ها و آیکون‌های محلی.
- به‌صورت سرویس systemd (`xui-sub-panel`) روی پورت ۸۰۸۰ اجرا می‌شود (قابل تغییر).
- احراز هویت با توکن Bearer؛ مقایسه با `hmac.compare_digest` (مقاوم در برابر
  timing attack). توکن با `nucsub webpanel token` بازیابی می‌شود.
- امکانات: فهرست/فعال‌سازی/بازنشانی/حذف قالب + تنظیم چنل تلگرام.
- وب‌پنل به‌صورت پیش‌فرض نصب/اجرا نمی‌شود؛ با `nucsub webpanel start` راه می‌افتد.

### ۶-۳ نصب‌کننده (`install.sh`)

- نصب یک‌دستوری با `curl` از روی GitHub.
- پرسش انتخاب پنل (یا خودکار با متغیر محیطی `NUC_SUB_PANEL`).
- فقط فایل‌های همان پنل را دانلود می‌کند.
- فقط **یک قالب پیش‌فرض (`gradient`)** را از قبل می‌گذارد؛ بقیه هنگام اولین
  apply دانلود می‌شوند (نصب سریع و سبک).
- حالت غیرتعاملی برای اتوماسیون: `NUC_SUB_PANEL=3xui XUI_SUB_NONINTERACTIVE=1`.
- پس از نصب، منوی تعاملی را باز می‌کند.

### ۶-۴ قالب‌ها

- ده‌ها قالب آماده: طراحی‌های کلاسیک `gradient، arctic، volt` به‌علاوهٔ ده‌ها
  قالب مدرن PasarGuard و 3x-ui (مثل `indigo، zenith، ocean، command`) — لیست
  کامل با `nucsub list`.
- پشتیبانی از راست‌به‌چپ فارسی و چپ‌به‌راست انگلیسی (تغییر زبان زنده در صفحه).
- تم روشن/تاریک، نمودار دونات مصرف، نشان نوع پروتکل، دکمه‌های کپی لینک/کپی همه،
  دسترسی سریع به کانفیگ اختصاصی اپ، دکمهٔ پشتیبانی و دکمهٔ تلگرام.
- هر دو نسخهٔ Go (3x-ui) و Jinja2 (Pasarguard) از همان طراحی‌ها استفاده می‌کنند
  (در مجموع ۳۳ قالب PasarGuard + ۲۳ قالب 3x-ui).

---

## ۷) امنیت

- توکن وب‌پنل در فایل `0600`؛ سرویس systemd متغیرهای محیطی را از فایل `0600` می‌خواند.
- همهٔ APIها نیاز به هدر `Authorization: Bearer <token>` دارند.
- هدرهای امنیتی روی همهٔ پاسخ‌ها: `nosniff`، `no-store`، `X-Frame-Options`.
- اعتبارسنجی سخت نام قالب با الگوی allowlist پیش از هر عملیات فایل/دیتابیس.
- سخت‌سازی systemd: `NoNewPrivileges=true` و `PrivateTmp=true`.
- بدون CDN خارجی؛ بدون وابستگی Node/npm در زمان اجرا — قالب‌ها آفلاین هم کار می‌کنند.

---

## ۸) نصب و شروع سریع

```bash
bash <(curl -Ls https://raw.githubusercontent.com/nuctereadev/NUC-SUB/main/install.sh)
```

یا غیرتعاملی:

```bash
NUC_SUB_PANEL=3xui XUI_SUB_NONINTERACTIVE=1 bash <(curl -Ls https://raw.githubusercontent.com/nuctereadev/NUC-SUB/main/install.sh)
```

پیش‌نیازها: دسترسی root، Bash 4+، `curl`، `sqlite3` (برای 3x-ui، نصب‌کننده خودش
نصب می‌کند)، Python3 (برای Pasarguard).

---

## ۹) توسعه

### افزودن قالب جدید 3x-ui

یک پوشهٔ `themes/<نام>/index.html` با ساختار Go template بسازید و سپس
`nucsub apply <نام>` — CLI به‌صورت خودکار آن را می‌شناسد و deploy می‌کند
(الزاماً هم‌چیدمان با قالب‌های موجود: فونت‌ها و آیکون‌ها لوکال‌اند).

### افزودن قالب جدید Pasarguard

یک فایل `pasarguard-themes/subscription/<نام>.html` (Jinja2 مستقل) بسازید و
apply کنید.

---

## ۱۰) وضعیت پروژه و تاریخچهٔ فنی (برای هوش مصنوعی علاقه‌مند به context)

پروژهٔ NUC-SUB بعد از یک نسخهٔ اولیهٔ Node/Express prototype (فایل‌های
`server.js` و `templates/*.ejs` که امروز مازاد و از مسیر اصلی حذف‌شده‌اند) به
معماری فعلی بازطراحی شد: **Bash CLI + Python وب‌پنل + قالب‌های Go template/Jinja2.**

نقاط عطف فعالِ اخیر:

- بازطراحی یکپارچهٔ ۱۶ قالب (زبان/تم زنده، نمودار مصرف، نشان پروتکل) و رفع باگ
  `{{ end }}` در قالب Go.
- نسخه‌بندی خودکار و دستور `update` (نسخهٔ فعلی 2.0.1).
- حل مشکلات پایداری وب‌پنل (سرویس systemd واقعی، مدیریت crash، اصلاح URL نمایشی).
- حذف کامل قابلیت Preview (که تصمیم به ساده‌سازی بود).
- نصب سریع: فقط تم پیش‌فرض دانلود می‌شود، بقیه به‌صورت تقاضامحور و موازی.
- **قابلیت چنل تلگرام (نزدیک‌ترین تغییر):** placeholder `@@TG_CHANNEL@@` در هر ۸
  قالب 3x-ui + منطق نمایش/مخفی‌سازی JS؛ بازنویسی injector تا از نسخهٔ pristine
  کپی کند و `nucsub telegram set/clear` و ذخیرهٔ وب‌پنل، تم فعال را فوراً به‌روز
  کنند؛ زیرمنوی Telegram در منوی Web panel برای مدیریت از ترمینال.

---

## ۱۱) قواعد کدنویسی (برای همکاری/بازبینی)

- CLI: Bash با `set -euo pipefail`؛ توابع کوتاه با نام‌گذاری `module_action`؛
  خروجی فارسی/انگلیسی رنگی با متغیرهای رنگی سراسری؛ همهٔ مسیرها متن‌پذیر.
- وب‌پنل: فقط کتابخانهٔ استاندارد Python3؛ هر API با توکن؛ هیچ ایمپورت خارجی.
- قالب‌ها: خودکفا (فونت/آیکون محلی)، راست‌به‌چپ، بدون CDN.
- نسخه‌بندی تمیز با کامیت‌های توصیفی؛ پیش از هر تغییر، تست روی سرور واقعی با
  `bash -n` (سرور مقصد) و `python3 -m py_compile`.

---

## ۱۲) خلاصه برای یک جلسهٔ معرفی کوتاه

> «NUC-SUB یک ابزار متن‌باز برای برندسازی صفحهٔ اشتراک VPN است. آن را با یک خط
> دستور روی پنل 3x-ui یا Pasarguard نصب می‌کنید، از ۸ قالب یکی را برمی‌گزینید
> (از ترمینال یا وب‌پنل) و به کاربران، صفحه‌ای مدرن با استفادهٔ ترافیک، تاریخ
> انقضا، لیست کانفیگ‌ها، دکمه‌ی تلگرام و ظاهر برند خودتان نمایش داده می‌شود —
> بدون دست‌زدن به امنیت پنل یا ساختار کانفیگ. نصب سریع، قابل‌ارتقا با یک دستور،
> و گسترش‌پذیر برای قالب‌های دلخواه.»

---

*نویسنده/نگهدارنده: NUCTEREA — [github.com/nuctereadev](https://github.com/nuctereadev)*